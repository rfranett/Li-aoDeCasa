#include <stdio.h>

int main(void)
{
    int cont;
     cont =1;

     for (cont;cont <= 3;cont= cont+1)
     { printf("nos dias de hoje não achamos mais os preços de antes\n");


     }

  return(0);
}